# GitHub Copilot instructions

The canonical repository instructions are in `AGENTS.md`. Read and follow that
file before proposing or applying changes.

Use repository-relative evidence instead of assuming prior chat context. Do
not duplicate the repository handbook in this adapter; update `AGENTS.md`
when a cross-tool rule changes.
